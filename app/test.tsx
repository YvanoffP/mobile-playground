import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";


const TestComponent = () => {
    return <SafeAreaView>
        <Text>Test View from TestComponent</Text>
    </SafeAreaView>
}

export default TestComponent;